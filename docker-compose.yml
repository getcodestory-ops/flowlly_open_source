version: "3.3"
services:
  frontend_app:
    image: node:22.18.0
    working_dir: /app
    volumes:
      - .:/app
    environment:
      - NODE_ENV=development
    ports:
      - "3000:3000"
    command: sh -c "yarn && yarn dev"
